<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.ItemDAO">
	<select id="totalC" parameterType="ItemDTO">
		SELECT 
			COUNT (*)
     	FROM 
      		TB_ITEM
	</select>
	
	<insert id="insertItem" parameterType="ItemDTO">
		INSERT INTO TB_ITEM(
			IT_CD
			, IMG_CD
			, IT_NM
			, PRICE
			, CAT
			, OPT_CD 
		) VALUES (
			TB_ITEM_SEQ.NEXTVAL
			, #{imgCd}
			, #{itNm}
			, #{price}
			, #{cat}
			, #{optCd}
		)
		<selectKey keyProperty="itCd" resultType="int">
			SELECT
				TB_ITEM_SEQ.CURRVAL
			FROM DUAL
		</selectKey>
	</insert>
	<select id="selectItem" parameterType="Integer" resultType="ItemDTO">
		SELECT
			IT_CD as itCd
			, IMG_CD as imgCd
			, IT_NM as itNm
			, PRICE as price
			, CAT as cat
			, OPT_CD as optCd
		FROM TB_ITEM
		WHERE
			IT_CD = #{itCd}
	</select>
	<select id="itemList" parameterType="String" resultType="ItemListDTO">
	SELECT

		IT_CD as itCd
		, IMG_CD as imgCd
		, IT_NM as itNm
		, PRICE as price
		, CAT as cat
		, OPT_CD as optCd
	FROM
		FROM TB_ITEM
	WHERE
		CAT = #{cat}
	</select>
</mapper>