<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.CartOrdDAO">
	<insert id="insertList" parameterType="ItemDTO">
		insert into tb_item_list values (TB_ITEM_SEQ.currval, TB_ITEM_LIST_SEQ.nextval)
		<foreach collection=""></foreach>
		<selectKey keyProperty="Item_Code" resultType="int" order="AFTER">
			select tb_item_seq.currval from dual
		</selectKey>
	</insert>
</mapper>